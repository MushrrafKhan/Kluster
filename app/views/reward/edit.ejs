<% layout('./../layouts/default') -%>
<div class="row">
    <div class="col-12">
        <div class="widget pad30-65">
            <h4 class="font-20 mb-30">Edit Reward </h4>
            <form method="post" action="/reward/update/<%=reward._id%>" id="editRewardPage">
                <div class="row">
                    <div class="form-group mb-3 col-lg-6">
                        <label>Enter Reward(In $):</label>
                        <input type="number" name="reward" class="form-control" placeholder="reward" id="reward" autocomplete="off" value="<%=reward.reward %>" required>
                    </div>
                </div> 
                <div class="col-lg-12 text-center">
                    <div class="form-group mb-3 text-center">
                        <button id="submitProfileForm" type="submit" class="btn btn-primary waves-effect waves-light">update</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.3/jquery.validate.min.js"></script>
    <script>
        $("#editRewardPage").validate({
  rules: {
    reward: {
      required: true,
    },
  },
  messages: {
    reward: {
      required: '<p style="color:red;">required</p>',
    },
  },
  submitHandler: function (form) {
    form.submit();
  },
});
    </script>